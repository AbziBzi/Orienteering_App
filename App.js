import React, { Component } from 'react';
import AppContainer from './app/config/AppNavigator';

export default class App extends Component {
  render() {
    return (
      <AppContainer
      />
    );
  }
}